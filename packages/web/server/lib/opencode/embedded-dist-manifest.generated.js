// Placeholder for source/package mode.
// The binary workflow overwrites this file after `bun run build:web` so Bun can
// embed the built dist files via static `with { type: "file" }` imports.
export const embeddedDistFiles = Object.freeze({});
